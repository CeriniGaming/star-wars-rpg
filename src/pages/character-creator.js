import React from 'react';
import ReactDOM from 'react-dom';
import CharacterCreator from '../components/CharacterCreator';

const node = document.getElementById('character-creator');
ReactDOM.render(<CharacterCreator />, node);
